# Implementation Plan: User Configuration Cleanup

**Branch**: `001-user-cleanup` | **Date**: 2026-03-31 | **Spec**: [spec.md](./spec.md)

**Input**: Feature specification from `/specs/001-user-cleanup/spec.md`

**Note**: This template is filled in by the `/speckit.plan` command. See `.specify/templates/plan-template.md` for the execution workflow.

## Summary

Clean up orphaned references to users "alice" and "bob" that were removed from the users/ directory but still exist in host configurations and documentation. Update hosts/unknown/ to use existing "glitch" user. Update documentation to reflect current system state while preserving historical records.

## Technical Context

**Language/Version**: Nix (nixos-unstable)  
**Primary Dependencies**: nixpkgs, home-manager, sops-nix, flake-parts  
**Storage**: N/A (declarative configuration)  
**Testing**: `nix flake check`, `nixos-rebuild build --flake .#unknown`  
**Target Platform**: Linux (NixOS)  
**Project Type**: NixOS flake configuration  
**Performance Goals**: Build time under 2 minutes  
**Constraints**: Must preserve historical records in specs/ and CHANGELOG.md  
**Scope**: Single host (hosts/unknown/), cleanup of 2 users (alice/bob)

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

| Gate | Status | Notes |
|------|--------|-------|
| Documentation Synchronization (Principle V) | ✅ PASS | Docs update is part of the feature requirement |
| Declarative by Default (Principle III) | ✅ PASS | Using Nix expressions only |
| Flake-Based Architecture (Principle II) | ✅ PASS | Changes flow through flake.nix |
| Validation Before Apply (Principle XII) | ✅ PASS | Will validate with `nix flake check` |

## Project Structure

### Documentation (this feature)

```text
specs/001-user-cleanup/
├── plan.md              # This file
├── spec.md              # Feature specification
├── research.md          # Phase 0 output (skipped - no unknowns)
├── data-model.md        # Phase 1 output
├── quickstart.md        # Phase 1 output
└── tasks.md             # Phase 2 output (/speckit.tasks)
```

### Source Code (repository root)

```text
nixos-config/
├── flake.nix            # Root flake (no changes needed)
├── hosts/
│   └── unknown/
│       └── config.nix   # Update defaultUsers from ["alice" "bob"] to ["glitch"]
├── users/
│   ├── default.nix      # Already working (dynamically discovers users)
│   └── glitch/
│       └── default.nix  # Existing user config (no changes needed)
├── docs/
│   └── end-user/
│       └── users.md     # Update examples to use "glitch" instead of "alice"/"bob"
└── specs/               # Preserve historical records (no changes)
```

**Structure Decision**: This is a cleanup task affecting:
- 1 host configuration file (hosts/unknown/config.nix)
- 1 documentation file (docs/end-user/users.md)

No new modules, profiles, or complex architecture changes required.

## Complexity Tracking

| Violation | Why Needed | Simpler Alternative Rejected Because |
|-----------|------------|-------------------------------------|
| N/A | N/A | N/A |

## Phase 0: Research

**Status**: SKIPPED

**Rationale**: This is a cleanup task with no unknowns. All required information is present in the feature spec:
- Target users (alice, bob) are clearly identified
- Target host (unknown) is specified
- Desired end state (glitch user configured) is documented
- Scope is limited to hosts/unknown/ only

## Phase 1: Design & Contracts

### Data Model

The data model is already defined by the existing flake structure. No changes needed.

### Interface Contracts

This project does not expose external interfaces (CLI, API, etc.) - it is a NixOS configuration. No contracts required.

### Quickstart

The cleanup task does not require a quickstart guide - users simply run:
```bash
nixos-rebuild build --flake .#unknown
```

After cleanup, the system should build successfully with the glitch user.

## Phase 2: Implementation Tasks

Tasks will be generated by `/speckit.tasks` command.

## Next Steps

1. Run `/speckit.tasks` to generate implementation tasks
2. Execute cleanup tasks in order
3. Validate with `nix flake check` and `nixos-rebuild build --flake .#unknown`
