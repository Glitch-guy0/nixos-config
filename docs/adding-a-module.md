# Adding a Module

1. Determine if it requires root (system) or user-space (home).
2. Create `module.nix` in the respective `modules/` directory.
3. Use the `lib.mkOption` pattern.
4. Include standard file header.
